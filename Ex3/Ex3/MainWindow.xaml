<Window x:Class="Ex3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ex3"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="375">
    <Grid>
        <DataGrid Name="info" Margin="0,0,167,-5" CanUserAddRows="True" CanUserResizeRows="True" CanUserSortColumns="True" AutoGenerateColumns="False" ItemsSource="{Binding}" IsReadOnly="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Width="*" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Surname" Width="*" Binding="{Binding Surname}"/>
                <DataGridTextColumn Header="Course" Width="*" Binding="{Binding Course}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Margin="230,80,27,205" Content="Сохранить" Click="Button_Click"></Button>
    </Grid>
</Window>
